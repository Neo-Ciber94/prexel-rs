Usage:
    eval [expression]
    eval [--option] [expression]

Options:
    eval [--help | --h]
        Explains how to use `eval` and show all the possibles options.

    eval [--run | --r] [--option]
        Executes the evaluator in a loop, providing
        advantages as declaring custom variables,
        and storing the last result in a variable
        called `result`.
        The type can be --decimal, --complex or --bigdecimal,
        `--decimal` is the default.

    eval [--decimal | --d] [expression]
        Evaluates a math expression using a 128 bits decimal.
        This is used by default.

    eval [--bigdecimal | --bd] [expression]
        Evaluates the given math expression using
        an arbitrary precision decimal number.

    eval [--complex | --c] [expression]
        Evaluates a math expression of complex numbers.

    eval [--context | --ctx] [--option]
        Display all the constants, functions and operator of the context.
        The type can be --decimal, --bigdecimal or complex.

    eval [--operators | --ops] [--option]
        Display all the operators of the context.
        The type can be --decimal, --bigdecimal or complex.

    eval [--functions | --funs] [--option]
        Display all the functions of the the context.
        The type can be --decimal, --bigdecimal or complex.

    eval [--constants | --cons] [--option]
        Display all the constants of the context.
        The type can be --decimal, --bigdecimal or complex.
